# Binary search

### What it does

Simple CLI command that creates a pre-sorted array of integers and asks the user for an input of an integer. The program uses binary search to find the index of the element that the user entered.

### Why I made this

Learning cpp and git from scratch.

### Compile command

```
g++ @compile_flags.txt src/*.cpp -o build/BinarySearch
```